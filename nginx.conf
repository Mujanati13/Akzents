# ============================================
# Nginx Reverse Proxy Configuration
# ============================================
# Place this in /etc/nginx/sites-available/akzente
# Then: sudo ln -s /etc/nginx/sites-available/akzente /etc/nginx/sites-enabled/
# Test: sudo nginx -t
# Reload: sudo systemctl reload nginx
# ============================================

# Backend API
server {
    listen 80;
    server_name api.yourdomain.com;

    location / {
        proxy_pass http://localhost:4010;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # File upload size
        client_max_body_size 50M;
    }
}

# HeadOffice (Admin Portal)
server {
    listen 80;
    server_name admin.yourdomain.com;

    location / {
        proxy_pass http://localhost:3010;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Client Portal
server {
    listen 80;
    server_name client.yourdomain.com;

    location / {
        proxy_pass http://localhost:3011;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Merchandiser App
server {
    listen 80;
    server_name app.yourdomain.com;

    location / {
        proxy_pass http://localhost:3012;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ============================================
# Single Domain Configuration (Alternative)
# ============================================
# If you want to use a single domain with different paths:
#
# server {
#     listen 80;
#     server_name yourdomain.com;
#
#     # API
#     location /api {
#         proxy_pass http://localhost:4010;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         client_max_body_size 50M;
#     }
#
#     # Admin Portal
#     location /admin {
#         proxy_pass http://localhost:3010;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#     }
#
#     # Client Portal
#     location /client {
#         proxy_pass http://localhost:3011;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#     }
#
#     # Merchandiser App (default)
#     location / {
#         proxy_pass http://localhost:3012;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#     }
# }
